type Query {
    bookById(id: ID): Book
    products: [Product]
}

type Book {
    id: ID
    name: String
    pageCount: Int
    author: Author
}

type Author {
    id: ID
    firstName: String
    lastName: String
}

type Product {
    id: ID
    name: String
    priceInPence: Int
}
